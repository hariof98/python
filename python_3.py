""" List Methods or List Functions"""

# list = [1, 2, 3, 3, 4, 5, 6]
# print(list)
# list.append(7)
# print(list)
# list.insert(1, 99)  # While using insert we need to define the index in which we are going to insert the new element (SYNTAX : insert(index, new_element)
# print(list)
#
# print(list.count(3))
# print(list.pop())  # Displays the last element in the list
# print(list.index(5))
#
# list.sort()  # displays elements in ascending order
# print(list)
#
# list2 = list.copy()
# print(list2)
#
# list.remove(3)   # removes the given value...if multiple occurrences, it will remove the first occurrence
# print(list)
#
# list.clear()       # deletes everything from the list
# print(list)



""" extend in list """

# smart_phones = ['Apple', 'Samsung', 'Pixel']
# os = ['ios', 'Android']
#
# smart_phones.extend(os)
# print(smart_phones)



""" TUPLES """

""" 
    Tuples and Lists are very identical
    Tuples are immutable(not changeable)
    we cannot use additional functions or methods like insert, append, .... in tuples
    we can only use len() and count() in Tuples
    Lists can be defined in [] and tuples can be defined in () 
"""

# tuples = (1, 2, 111, 222, 000)
# print(tuples.count(2))
# print(len(tuples))
# print(tuples)

